services:
  app:
    build: .
    container_name: office-frontend
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "5010"
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: amp_portal_backend_app-network
